import tw from 'tailwind-styled-components';

const Container = tw.div`container px-4 max-w-6xl mx-auto`;
const Background = tw.div`bg-neutral-900 text-neutral-50 py-12`;
const Title = tw.h1`text-3xl mb-32 font-semibold uppercase tracking-widest`;

export { Container, Background, Title };
